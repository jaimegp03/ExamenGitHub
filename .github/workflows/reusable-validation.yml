name: Reusable Validation

on:
  workflow_call:
    
jobs:
    lint:
        runs-on: ubuntu-latest
        permissions:
            contents: read
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            
            - name: Run Linter
              run: echo "Ejecutando Linters y analisis estatico"
                
